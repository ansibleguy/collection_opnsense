---

- name: Testing Access Privilege
  hosts: localhost
  gather_facts: no
  module_defaults:
    group/oxlorg.opnsense.all:
      firewall: "{{ lookup('ansible.builtin.env', 'TEST_FIREWALL') }}"
      api_credential_file: "{{ lookup('ansible.builtin.env', 'TEST_API_KEY') }}"
      ssl_verify: false

    oxlorg.opnsense.list:
      target: 'privilege'

  tasks:
    - name: Listing
      oxlorg.opnsense.list:
      register: opn_pre1
      failed_when: >
        'data' not in opn_pre1 or
        opn_pre1.data  | length == 0
      when: false

    - name: Adding 1 - failing because unknown privilage
      oxlorg.opnsense.privilege:
        privilege: ANSIBLE_TEST_1_1
      register: opn_fail1
      failed_when: not opn_fail1.failed

    - name: Adding Dummy User
      oxlorg.opnsense.user:
        name: '{{ item }}'
      loop:
        - ANSIBLE_TEST_1_1
        - ANSIBLE_TEST_1_2

    - name: Adding Dummy Group
      oxlorg.opnsense.group:
        name: '{{ item }}'
      loop:
        - ANSIBLE_TEST_1_1
        - ANSIBLE_TEST_1_2

    - name: Granting 1
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user: ANSIBLE_TEST_1_1
        group: ANSIBLE_TEST_1_1
      register: opn1
      failed_when: >
        opn1.failed or
        not opn1.changed

    - name: Granting 1 - nothing changed
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user: ANSIBLE_TEST_1_1
        group: ANSIBLE_TEST_1_1
      register: opn2
      failed_when: >
        opn2.failed or
        opn2.changed
      when: not ansible_check_mode

    - name: Revoking - does not exist
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user: ANSIBLE_TEST_1_2
        group: ANSIBLE_TEST_1_2
        state: absent
      register: opn3
      failed_when: >
        opn3.failed or
        opn3.changed
      when: not ansible_check_mode

    - name: Revoking 1
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user: ANSIBLE_TEST_1_1
        group: ANSIBLE_TEST_1_1
        state: absent
      register: opn4
      failed_when: >
        opn4.failed or
        not opn4.changed
      when: not ansible_check_mode

    - name: Revoking 1 - nothing changed
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user: ANSIBLE_TEST_1_1
        group: ANSIBLE_TEST_1_1
        state: absent
      register: opn5
      failed_when: >
        opn5.failed or
        opn5.changed
      when: not ansible_check_mode

    - name: Granting 2
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user:
          - ANSIBLE_TEST_1_1
          - ANSIBLE_TEST_1_2
        group:
          - ANSIBLE_TEST_1_1
        state: pure
      register: opn6
      failed_when: >
        opn6.failed or
        not opn6.changed

    - name: Granting 2 - nothing changed
      oxlorg.opnsense.privilege:
        privilege: user-config-readonly
        user:
          - ANSIBLE_TEST_1_1
          - ANSIBLE_TEST_1_2
        group:
          - ANSIBLE_TEST_1_1
        state: pure
      register: opn7
      failed_when: >
        opn7.failed or
        opn7.changed
      when: not ansible_check_mode

    - name: Cleanup Dummy User
      oxlorg.opnsense.user:
        name: '{{ item }}'
        state: 'absent'
      loop:
        - ANSIBLE_TEST_1_1
        - ANSIBLE_TEST_1_2
      when: not ansible_check_mode

    - name: Adding Dummy Group
      oxlorg.opnsense.group:
        name: '{{ item }}'
        state: 'absent'
      loop:
        - ANSIBLE_TEST_1_1
        - ANSIBLE_TEST_1_2
      when: not ansible_check_mode
